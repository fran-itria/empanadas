---
import Layout from "../layouts/Layout.astro";
import { ConfirmOrder } from "../components/confirmOrder/ConfirmOrder.tsx";
const { order } = Astro.params;
const orderArray = order?.split("-");
---

<Layout>
  <ConfirmOrder client:visible orderArray={orderArray} />
</Layout>