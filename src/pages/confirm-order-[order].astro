---
import Layout from "../layouts/Layout.astro";
import Form from "../components/Form.tsx";
const { order } = Astro.params;
const orderArray = order?.split("-");
---

<Layout>
  <Form client:visible orderArray={orderArray} />
</Layout>
