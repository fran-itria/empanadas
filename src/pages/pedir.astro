---
import { empanadas } from "../conts.ts";
import { Card } from "../components/Card.tsx";
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <main>
    <h1>Quieres realizar un pedido?</h1>
    <p>
      Los pedidos se toman de a media docena o docena,
      <b>no se venden empanadas individuales</b>, 
      por lo que el contador es de a media docena o docena.
    </p>
    <p style={{ marginTop: "10px", marginBottom: "20px" }}>
      Estas son nuestras variedades:
    </p>
    <div>
      {
        empanadas.map((empanada) => {
          return (
            <Card
              title={empanada.title}
              stock={empanada.stock}
              option={empanada.option}
            />
          );
        })
      }
    </div>
  </main>
</Layout>

<style>
  div {
    display: grid;
    grid-template-columns: auto auto auto;
    gap: 10px;
    width: 50vw;
  }
  p {
    margin: 0;
  }
</style>
